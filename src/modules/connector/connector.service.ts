/**
 * =============================================================================
 * connector.service.ts - Service cho API Connector
 * =============================================================================
 *
 * Service placeholder cho ConnectorModule.
 * Hiện tại chưa có logic riêng vì ConnectorController sử dụng trực tiếp
 * các service từ module khác (CdrService, GroupsService, UsersService).
 *
 * Trong tương lai có thể thêm logic đặc thù cho connector tại đây.
 */

import { Injectable } from '@nestjs/common';

@Injectable()
export class ConnectorService {}
